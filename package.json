{
  "name": "mysecondcountry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "My Second Country: cited 'where should I live' relocation intelligence engine.",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "pnpm -r --if-present typecheck",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "format": "biome format --write .",
    "verify:data": "tsx scripts/check-freshness.ts && tsx scripts/assert-uniqueness.ts",
    "verify:sources": "tsx scripts/check-sources.ts",
    "indexnow": "tsx scripts/submit-indexnow.ts",
    "verify:build": "tsx scripts/assert-fence.ts && tsx scripts/assert-fence-before-claim.ts && tsx scripts/assert-h1-count.ts && tsx scripts/assert-table-semantics.ts && tsx scripts/assert-confidence-marks.ts && tsx scripts/assert-no-em-dashes.ts && tsx scripts/assert-no-individualised-copy.ts && tsx scripts/validate-jsonld.ts && tsx scripts/assert-date-modified.ts && tsx scripts/assert-faq-jsonld.ts && tsx scripts/assert-robots.ts && tsx scripts/check-a11y.ts",
    "digest": "tsx scripts/digest.ts",
    "prepare": "simple-git-hooks || true"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged && node scripts/secret-scan.mjs --staged"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc}": "biome check --write --no-errors-on-unmatched"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.10.0",
    "@where/data": "workspace:*",
    "@where/engine": "workspace:*",
    "axe-core": "^4.10.2",
    "jsdom": "^25.0.1",
    "lint-staged": "^15.2.10",
    "simple-git-hooks": "^2.11.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
