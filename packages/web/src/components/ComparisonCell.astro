---
import type { CitedValue } from "@where/data";
import ConfidenceMark from "./ConfidenceMark.astro";

interface Props {
  cited: CitedValue;
  displayValue: string;
  unit?: string;
  subtext: string;
  country: "gr" | "pt" | "es";
  isLeader: boolean;
  score: number;
  sourceRef: number;
}

const { cited, displayValue, unit, subtext, country, isLeader, score, sourceRef } = Astro.props;

const COUNTRY_FLAG: Record<string, string> = {
  gr: "🇬🇷",
  pt: "🇵🇹",
  es: "🇪🇸",
};
const COUNTRY_LABEL: Record<string, string> = {
  gr: "Greece",
  pt: "Portugal",
  es: "Spain",
};

const flagLabel = COUNTRY_FLAG[country] ?? "";
const countryLabel = COUNTRY_LABEL[country] ?? country;
---

<div class={`country-cell country-cell--${country}`} data-country={country}>
  <!-- Mobile flag label -->
  <span class="mobile-flag">{flagLabel} {countryLabel}</span>

  <!-- Value row -->
  <div class="value-row">
    <span class={`value-num value-num--${country}`}>{displayValue}</span>
    {unit && <span class="value-unit">{unit}</span>}
    {isLeader && (
      <span class={`value-arrow value-arrow--${country}`} aria-label="leads on this metric">
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="7" y1="17" x2="17" y2="7"/>
          <polyline points="7 7 17 7 17 17"/>
        </svg>
      </span>
    )}
  </div>

  <!-- Subtext -->
  <p class="cell-subtext">{subtext}</p>

  <!-- Badges row -->
  <div class="cell-badges">
    <ConfidenceMark level={cited.confidence} />
    <a href={`#sources-${sourceRef}`} class="src-ref">[{sourceRef}]</a>
  </div>

  {cited.confidence === "low" && (
    <p class="low-conf-note">Reported, not verified against a primary source.</p>
  )}

  <!-- Score bar -->
  <div class="bar-wrap">
    <div
      class={`bar-fill bar-fill--${country}`}
      data-score={score}
      style={`opacity: ${isLeader ? 1 : 0.42}`}
    ></div>
  </div>
</div>

<style>
  .country-cell {
    padding: 24px 12px;
    border-left: 1px solid var(--hairline-soft);
    transition: opacity 0.2s var(--ease);
  }
  @media (min-width: 640px) {
    .country-cell {
      padding: 24px 16px;
    }
  }
  @media (min-width: 1024px) {
    .country-cell {
      padding: 24px;
    }
  }

  .mobile-flag {
    display: block;
    font-family: var(--f-ui);
    font-size: 9px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--stone);
    margin-bottom: 8px;
  }
  @media (min-width: 640px) {
    .mobile-flag {
      display: none;
    }
  }

  .value-row {
    display: flex;
    align-items: flex-end;
    gap: 4px;
    flex-wrap: wrap;
    line-height: 1;
  }

  .value-num {
    font-family: var(--f-mono);
    font-size: clamp(1.4rem, 2.5vw, 2.1rem);
    font-weight: 600;
    line-height: 1;
    font-variant-numeric: tabular-nums;
    color: var(--ink);
  }
  .value-num--gr { color: var(--ink); }
  .value-num--pt { color: var(--ink); }
  .value-num--es { color: var(--ink); }

  .value-unit {
    font-family: var(--f-ui);
    font-size: 10px;
    font-weight: 500;
    color: var(--stone);
    margin-bottom: 2px;
  }

  .value-arrow {
    margin-bottom: 2px;
    opacity: 0.85;
    display: inline-flex;
    align-items: flex-end;
    color: var(--pop);
  }
  .value-arrow--gr { color: var(--pop); }
  .value-arrow--pt { color: var(--pop); }
  .value-arrow--es { color: var(--pop); }

  .cell-subtext {
    font-size: 11px;
    color: var(--stone);
    margin: 6px 0 0;
    line-height: 1.3;
    display: none;
  }
  @media (min-width: 640px) {
    .cell-subtext {
      display: block;
    }
  }

  .cell-badges {
    display: none;
    align-items: center;
    gap: 8px;
    margin-top: 8px;
    flex-wrap: wrap;
  }
  @media (min-width: 640px) {
    .cell-badges {
      display: flex;
    }
  }

  .src-ref {
    display: inline-flex;
    align-items: center;
    font-family: var(--f-mono);
    font-size: 10px;
    color: var(--stone);
    text-decoration: none;
    transition: color 0.15s var(--ease);
  }
  .src-ref:hover {
    color: var(--ink);
  }
  .src-ref:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }

  .low-conf-note {
    margin: 6px 0 0;
    font-size: 12px;
    color: var(--stone);
    line-height: 1.4;
  }

  .bar-wrap {
    margin-top: 12px;
    height: 2px;
    width: 100%;
    overflow: hidden;
    background: var(--hairline-soft);
  }
  .bar-fill {
    height: 100%;
    width: 0%;
    transition: width 1.1s cubic-bezier(0.16, 1, 0.3, 1) 0.2s;
    background: var(--accent);
  }
  .bar-fill--gr { background: var(--accent); }
  .bar-fill--pt { background: var(--accent); }
  .bar-fill--es { background: var(--accent); }
</style>
