# /dossier

The cited-dossier fake-door (ADR-0018 entry product). A wiring-first product page describing the one-time, dated, cited relocation snapshot, with a single CTA that fires `dossier_fakedoor_click` and reuses the EmailCapture wiring (hidden `source=dossier`) to collect intent. No payment integration. Discoverable from the sources surface and the sitemap. The design thread styles it later.
